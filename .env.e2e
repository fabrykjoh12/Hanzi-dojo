# E2E mode config — NOT secrets. These point the app at a mock Supabase host
# that the Playwright fixtures intercept (tests/fixtures/mockSupabase.js).
# Loaded by `vite --mode e2e` (see the dev:e2e npm script). Safe to commit.
VITE_SUPABASE_URL=https://mock.supabase.co
VITE_SUPABASE_ANON_KEY=mock-anon-key-not-a-secret

# The e2e account's email, so the Playwright run can reach /dev — where the
# P14-1 control gallery lives (p14-controls.spec.js measures it). The real
# allowlist default is a single maintainer address; this only applies to
# `vite --mode e2e`, never to a production build.
VITE_DEV_EMAILS=test@example.com
